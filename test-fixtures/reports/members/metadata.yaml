id: members
title: Members (Sample)
description: "Minimal single-table sample report used as a shared test fixture across report-engine's modules. Not a real production report."
outputFormats: [pdf, xlsx]

parameter:
    - name: p_joined_after
      label: Joined after
      description: Only include members who joined on or after this date
      comment: Optional filter, defaults to no lower bound when omitted.
      type: date

versionHistory:
    - version: "1.0"
      createdOn: 2026-09-04
      description: Initial version, created as a shared test fixture.
