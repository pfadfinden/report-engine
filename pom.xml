<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>de.pfadfinden.reports_engine</groupId>
    <artifactId>reports-engine</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <properties>
        <!-- 7.x can no longer load *.jrxml/*.jrtx authored under 6.x - do not release/deploy until mv_reports' content is re-saved via Jaspersoft Studio 7+. -->
        <jasperreports.version>7.0.8</jasperreports.version>
    </properties>

    <modules>
        <module>executor</module>
        <module>azure-report-executor</module>
        <module>preprocessor</module>
        <module>local-report-executor</module>
    </modules>

    <build>
        <pluginManagement>
            <plugins>
                <!-- Without this, Maven falls back to the ancient default 2.17, which predates
                     JUnit 5 and silently discovers zero tests instead of failing loudly. -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.5.6</version>
                </plugin>
                <!-- Report-only for now: no coverage threshold is enforced yet, given how little
                     of the codebase has tests. Gives a baseline to track as that improves. -->
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>0.8.13</version>
                    <executions>
                        <execution>
                            <id>prepare-agent</id>
                            <goals>
                                <goal>prepare-agent</goal>
                            </goals>
                        </execution>
                        <execution>
                            <id>report</id>
                            <phase>test</phase>
                            <goals>
                                <goal>report</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <!-- Bound to compile (not verify): CI only runs `mvn package`, which reaches
                     compile but not verify, so binding here is what makes this an actual gate
                     rather than a check nobody runs - same reasoning as SpotBugs above. -->
                <plugin>
                    <groupId>com.diffplug.spotless</groupId>
                    <artifactId>spotless-maven-plugin</artifactId>
                    <version>2.44.3</version>
                    <configuration>
                        <java>
                            <googleJavaFormat />
                        </java>
                    </configuration>
                    <executions>
                        <execution>
                            <id>check-formatting</id>
                            <phase>compile</phase>
                            <goals>
                                <goal>check</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>